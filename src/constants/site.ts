/** Branch360 — adjust if the client provides a dedicated contact URL */
export const CONTACT_URL = 'https://www.branch360.co'

/** Same as main site `/img/logo.png`; bundled under `public/img/logo.png` for offline deploys */
export const BRANCH360_HOME = CONTACT_URL

export const BRANCH360_LOGO_SRC = `${import.meta.env.BASE_URL}img/logo.png`
